# 哨兵环境重装流程

创建者: cmyhj
创建时间: 2024年12月30日 21:11
上次编辑时间: 2025年3月21日 16:40
上次编辑者: cmyhj
tag: 教程
组别: 视觉

1. 安装梯子
    1. 推荐一个[Releases · clash-verge-rev/clash-verge-rev (github.com)](https://github.com/clash-verge-rev/clash-verge-rev/releases)
    2. 配置终端代理：在梯子内查看端口号
    
    ```bash
    export https_proxy=http://127.0.0.1:<端口>
    export http_proxy=http://127.0.0.1:<端口>
    export all_proxy=socks5://127.0.0.1:<端口>
    ```
    
2. 安装远程桌面串流sunshine:
    1. [Releases · LizardByte/Sunshine](https://github.com/LizardByte/Sunshine/releases) 
    
    ```bash
    sudo dpkg -i sunshine-ubuntu-22.04-amd64.deb
    ```
    
    1. 双击运行，设置账号：ckyf+nuc编号 密码：rmnuaa
    2. 配置如下设置
        
        ![image.png](image.png)
        
    3. 同一局域网（不用连接外部网络）下电脑端（或者手机端）moonlight连接
    4. 插HDMI诱骗器
    5. 如果sunshine没自动配置开机自启，写启动脚本加入开机自启
3. 编写自启的sh文件并添加自启
    
    ```bash
    touch remote_desktop.sh
    gedit remote_desktop.sh
    ```
    
    添加：
    
    ```bash
    #! /bin/bash
    gtk-launch sunshine
    ```
    
    给权限 
    
    ```bash
    chmod +x remote_desktop.sh
    ```
    
    加入自起脚本
    
    ![图片.png](%E5%9B%BE%E7%89%87.png)
    
4. 换源&安装ROS2
    
    ```bash
    wget http://fishros.com/install -O fishros && . fishros
    ```
    
5. 配置本地ssh
    1. 下载并配置git  
        
        ```bash
        sudo apt install git
        git config --global user.name "Your Name"
        git config --global user.email "your_email@example.com"
        git config --global http.proxy http://127.0.0.1:<端口>#<步骤1.b.处的代理服务器地址>
        git config --global https.proxy https://127.0.0.1:<端口>#<步骤1.b.处的代理服务器地址>
        ```
        
        可通过查看代理服务器地址与端口
        
        ```bash
        echo $http_proxy
        echo $https_proxy
        ```
        
    2. 生成新的SSH密钥，一路Enter即可
        
        ```bash
        ssh-keygen -t rsa -C "github注册@邮箱.com"
        ```
        
    3. 复制下面得到的密钥至githubSSHkeys
        
        ```bash
        cd ~/.ssh
        cat id_rsa.pub
        ```
        
        ![屏幕截图 2025-02-22 130140.png](%E5%B1%8F%E5%B9%95%E6%88%AA%E5%9B%BE_2025-02-22_130140.png)
        
    4. 换ssh端口
        
        ```bash
        gedit ~/.ssh/config
        ```
        
        添加
        
        ```bash
        Host github.com
          HostName ssh.github.com
          Port 443
          User git
        ```
        
6. 安装vscode并登录
    
    ```bash
    wget http://fishros.com/install -O fishros && . fishros
    ```
    
7. 开启性能模式，设置节电选项，设置电源按键行为，开启自动登录
    
    ![图片.png](%E5%9B%BE%E7%89%87%201.png)
    
    ![图片.png](%E5%9B%BE%E7%89%87%202.png)
    
8. 关闭自动休眠
    
    ```bash
    sudo systemctl mask sleep.target suspend.target
    #查看状态休眠模式确认，sleep状态为masked,表示自动休眠模式已关闭。
    systemctl status sleep.target
    ```
    
    参考：[https://blog.csdn.net/qq_38429958/article/details/131763351?sharetype=blog&shareId=131763351&sharerefer=APP&sharesource=2301_79971666&sharefrom=link](https://blog.csdn.net/qq_38429958/article/details/131763351?sharetype=blog&shareId=131763351&sharerefer=APP&sharesource=2301_79971666&sharefrom=link)
    
9. 开虚拟内存
    
    ```bash
    # 查看当前虚拟内存大小
    free -m 
    
    #新建目录存放swapfile
    sudo mkdir /swap
    cd /swap
    #生成swapfile文件
    sudo dd if=/dev/zero of=swapfile bs=1024 count=7000000
    #将生成的文件转换成swap文件
    sudo mkswap swapfile
    #激活swap文件
    sudo swapon swapfile
    
    #修改为随系统启动自动挂载生效
    sudo gedit /etc/fstab
    
    ！！！在最后追加一行：/swap/swapfile swap swap defaults 0 0
    ```
    
    来自参考：[https://blog.csdn.net/WU2629409421perfect/article/details/118878100](https://blog.csdn.net/WU2629409421perfect/article/details/118878100)
    
10. 设置按键一下立刻关机（改完了之后按开机键可以关机）
    
    ```bash
    sudo touch /etc/acpi/events/powerbtn
    sudo gedit /etc/acpi/events/powerbtn
    ```
    
    在其中添加以下内容：
    
    ```bash
    event=button/power
    action=/sbin/poweroff
    ```
    
    重启acpid服务，让修改生效：
    
    ```bash
    sudo systemctl restart acpid
    ```
    
    [https://blog.csdn.net/weixin_44517278/article/details/131008835](https://blog.csdn.net/weixin_44517278/article/details/131008835)
    
11. 卸载brltty 
    
    ```bash
    sudo apt remove brltty
    ```
    
12. 给串口权限
    
    ```bash
    sudo gedit /etc/udev/rules.d/70-ttyusb.rules
    ```
    
    在该文件中添加如下一行（可能不存在此文件而创建一个新文件）
    
    ```bash
    KERNEL==“ttyUSB[0-9]*”, MODE=“0666”
    ```
    
    来自参考：[https://blog.csdn.net/Android_WPF/article/details/120892617?ops_request_misc=%257B%2522request%255Fid%2522%253A%2522170914546316800186534822%2522%252C%2522scm%2522%253A%252220140713.130102334..%2522%257D&request_id=170914546316800186534822&biz_id=0&utm_medium=distribute.pc_search_result.none-task-blog-2~all~sobaiduend~default-2-120892617-null-null.142^v99^control&utm_term=ubuntu串口权限&spm=1018.2226.3001.4187](https://blog.csdn.net/Android_WPF/article/details/120892617?ops_request_misc=%257B%2522request%255Fid%2522%253A%2522170914546316800186534822%2522%252C%2522scm%2522%253A%252220140713.130102334..%2522%257D&request_id=170914546316800186534822&biz_id=0&utm_medium=distribute.pc_search_result.none-task-blog-2~all~sobaiduend~default-2-120892617-null-null.142%5Ev99%5Econtrol&utm_term=ubuntu%E4%B8%B2%E5%8F%A3%E6%9D%83%E9%99%90&spm=1018.2226.3001.4187)  
    
13. USB口重命名为sentry_serial 
    1. 查看要改名的USB的ID
        
        ```bash
        lsusb #拔插usb确定哪一个是c板
        cd etc/udev/rules.d
        sudo touch sentry.rules
        sudo gedit sentry.rules
        ```
        
    2. 在里面输入： 其中的“067b”，"2303"为usb线的ID
        
        ```bash
        KERNEL=="ttyUSB*", ATTRS{idVendor}=="067b", ATTRS{idProduct}=="2303", MODE:="0777", SYMLINK+="sentry_serial "
        ```
        
    
    来自参考：[https://blog.csdn.net/xx970829/article/details/115529204](https://blog.csdn.net/xx970829/article/details/115529204)
    
14. 安装nav2
    
    ```bash
    sudo apt install ros-humble-nav2-*
    ```
    
15. livox驱动 
    
    ```bash
    mkdir -p Livox/src
    cd Livox/src
    ```
    
    1. livox_sdk 
        
        ```bash
        git clone https://github.com/Livox-SDK/Livox-SDK2.git
        cd ./Livox-SDK2/
        mkdir build
        cd build
        cmake .. && make -j
        sudo make install
        cd ..
        cd ..
        ```
        
    2. livox_driver2修改版 
        
        ```bash
        git clone git@github.com:nuaa-rm/livox_ros_driver2.git
        cd ..
        source /opt/ros/humble/setup.sh
        sudo rosdep init
        rosdep update
        rosdep install --from-paths src --ignore-src -r -y
        cd src/livox_ros_driver2/
        ./build.sh humble
        ```
        
    3. 修改下面的路径并添加到.bashrc中: 
        
        ```bash
        source "path/to/livox_ws"/install/setup.bash
        ```
        
16. 安装pcl-tools 
    
    ```bash
    sudo apt install libpcl-dev
    sudo apt install pcl-tools
    ```
    
17. 安装rqt-tf-tree 
    
    ```bash
    sudo apt install ros-humble-rqt-tf-tree 
    #如果安装完了但是rqt不显示，要清除rqt缓存
    rm ~/.config/ros.org/rqt_gui.ini
    ```
    
18. 开启时间戳同步
    1. 查网卡是否支持软件/硬件时间戳功能
        1. 查看有哪些网卡 
            
            ```bash
            sudo apt install net-tools
            ifconfig
            ```
            
        2. 查询网卡 (如‘eth0’） 的状态 
            
            ```bash
            sudo apt install ethtool
            ethtool -T eth0
            ```
            
            网卡capabilities选项包括以下参数, 表明网卡支持软件时间戳：
            
            ```bash
            software-system-clock
            software-transmit
            software-receive
            ```
            
            tx,rx都要有才行
            
            网卡capabilities选项包括以下参数, 表明网卡支持硬件时间戳：
            
            ```bash
            SOF_TIMESTAMPING_RAW_HARDWARE
            SOF_TIMESTAMPING_TX_HARDWARE
            SOF_TIMESTAMPING_RX_HARDWARE
            ```
            
    2. 下载linuxptp-3.1.1 [Download linuxptp-3.1.1.tgz (linuxptp) (sourceforge.net)](https://sourceforge.net/projects/linuxptp/files/v3.1/linuxptp-3.1.1.tgz/download)
    3. 编译和安装: 
        
        ```bash
        cd linuxptp-
        make
        sudo make install
        ```
        
    4. 经测试，应该使用下面的命令启动时间戳软件同步，建议写入导航自启脚本： 
        
        ```bash
        sudo ptp4l -i eth0 -S -ml 6
        ```
        
        参考原文如下：
        
        运行如下指令，采用PTP模式启动ptp4l master时钟功能：
        
        `sudo ptp4l -i eth0 -l 6 -m`
        
        运行如下指令，采用gPTP模式软件时间戳启动ptp4l master时钟功能：
        
        `sudo ptp4l -i eth0 -S -ml 6 -f automotive-master.cfg`
        
        运行如下指令，采用gPTP模式硬件时间戳启动ptp4l master时钟功能：
        
        `sudo ptp4l -i eth0 -H -ml 6 -f automotive-master.cfg`
        
19. 拉烧饼仓库 
    
    ```bash
    mkdir -p auto_sentry2025/src/
    cd auto_sentry2025/src/
    git clone git@github.com:nuaa-rm/auto_sentry2025.git
    cd ..
    rosdep install --from-paths src --ignore-src -r -y
    ```
    
20. 修改有线连接，并连接雷达测试
    
    ![image.png](image%201.png)
    
21. 自瞄部署，见： [https://ckyf.org.cn/article/Environments-Build](https://ckyf.org.cn/article/Environments-Build)
22. 附：
    1. [四元数欧拉角在线转换计算器工具 - 爱图工具箱](https://www.itutool.com/tools/euler-quaternion-converter)